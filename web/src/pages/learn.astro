---
import Base from '../layouts/Base.astro';
import Construction from '../components/Construction.astro';

const description =
    'How a Grid Point Code is built: the serpentine over the world, the reflection that '
    + 'keeps the order continuous, and the one reset that makes the short form recoverable.';
---

<Base title="How it is built" description={description}>
    <Construction />
</Base>
